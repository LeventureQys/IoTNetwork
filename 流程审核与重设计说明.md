# 流程简化与重设计说明（beta v1.1）

- 创建日期：2026-08-19
- 审核对象：`流程说明.md`（v1.0 五阶段复杂流程，已归档）、`demo/` 双端实现
- 触发来源：用户需求《配网流程简化需求》（`Document/Update/beta v1.1/需求说明.md`）：
  “v1.0 写的相对较为复杂，v1.1 实际上是一个简化版本……只考虑设备一对一的情况，
  PC 端会主动打开一个名称为 Modu_xxx 的 wifi……PC 端程序会在 5935 端口开启 tcp 服务器，
  device 端主动尝试连接到指定 ip 的指定端口的 tcp，建立握手、心跳包连接。”
- 决策记录：`Document/Update/beta v1.1/问题清单.md`、`阶段二问题清单.md`、`阶段三问题清单.md`

## 一、审核结论

v1.0 五阶段流程（设备 SoftAP 配网 → 三级服务发现 → TCP 会话 → 异常自愈）面向的是
“多设备、动态网络、现场灵活配网”的通用场景，机制完备但业务层开销大。beta v1.1 的
实际场景收敛为：**一台 PC 固定服务一台设备、网络环境固定**，因此配网、服务发现与
多设备管理三个整段业务失去存在前提，全部裁剪；同时发生**角色反转**——由 PC 主动
创建热点、设备被动扫描连接，地址固定约定后连“发现上位机”的环节也不再需要。

结论：主链路从“五阶段 + 管理维度”收敛为 **PC 三段启动（热点 → 固定 IP → TCP）+
设备六态状态机（BOOT/WIFI_SCAN/STA_JOIN/CONNECT/SESSION/HEAL）**，协议命令从
14 条收敛到 5 条。硬件抽象层（`net_backend_t`）接口布局保持不变，仅后端语义调整。

## 二、裁剪对照

### 2.1 业务模块

| v1.0 模块 | v1.1 处理 |
|-----------|-----------|
| 设备 SoftAP 热点 + PIN 会话认证 + 凭据下发（`device_provision.c/.h`） | 已从构建图移除并物理删除；热点/认证/下发命令全部废弃 |
| mDNS / UDP 组播 / 候选列表服务发现（`device_discovery.c/.h`） | 同上；PC 端 `host_announcer`/`host_mdns`/`host_provision_client` 保留文件但不参与编译 |
| 多设备注册表与容量管理 | 改为一对一：`PROTO_HOST_MAX_CONN=1`，超限 `host_ack busy single_device_only` |
| 设备热点运行时（Linux hostapd/dnsmasq） | Linux 后端改为**纯 STA**（`device_linux_backend_create` 不再创建 `linux_hotspot_t`），普通用户无需 root；`linux_hotspot*` 源码保留供独立模块测试 |
| 诊断上报（`diag_query`/`diag_report`） | 删除命令；保留本地日志文件（`--log-dir`） |
| ESP32-C2 开发与验收 | 移出版本范围；`backends/esp32c2` 与 `firmware/esp32c2` 骨架保留、禁止修改 |

### 2.2 协议命令（14 → 5）

删除 `auth`、`auth_result`、`wifi_config`、`wifi_result`、`close_ap`、`host_announce`、
`host_bye`、`diag_query`、`diag_report`；保留 `device_hello`、`host_ack`、`ping`、
`pong`、`app_data`。报文字段同步精简（`device_hello` 删 `type/capabilities`；
`host_ack` 删 `server_time/fw_min_req`）。

### 2.3 状态机（七态 → 六态）

`AP_PROVISION`、`DISCOVERY` 删除；新枚举 `BOOT/WIFI_SCAN/STA_JOIN/CONNECT/SESSION/HEAL`，
两端 `protocol.h` 一致，`device_states` 写入 protocol-contract schema 2。

### 2.4 集成测试场景

旧场景（首次配网 / 优雅退出 / 关 AP 失败 / wifi_result 失败）删除，替换为
`b_hotspot_session / c_pc_stop / d_reconnect / e_second_device / f_no_target /
g_app_data / h_frame_probe`（场景 A 为契约一致性检查），见
`demo/integration_tests/README.md`。

## 三、角色反转与新建能力

| 能力 | 说明 |
|------|------|
| PC 主动热点（Windows） | 新增 `win_hotspot.h/.cpp`：仅允许现代移动热点 WinRT 路径（`StartTetheringAsync` 等），禁止 `netsh hostednetwork` 回退；SSID/密码设置、状态查询、固定 IP 配置 |
| PC 启动编排 | `HostApp::Start()` 顺序：校验配置 → 启动热点 → 校验/修正热点 IPv4 `192.168.137.1/24` → 监听 `0.0.0.0:5935`；任一步失败按逆序回滚（fail-closed，不回退 sim） |
| sim 双进程目录（schema 2） | PC 原子发布/删除 `pc-hotspot.json`；设备只读并将 `192.168.137.1:5935` 翻译到 loopback，双进程无需真实网卡 |
| 设备纯 STA（Linux） | `linux_wifi_create(sta_interface, ops)` 解除对 hotspot 的创建期耦合；vtable 的 `wifi_ap_start/stop` 返回 `DEMO_ERR`，状态机不调用热点/`tcp_listen`/mDNS/UDP |
| 一对一 TCP | 在线 + 待握手连接总数 ≤ 1；同设备旧连接离线才允许恢复；busy 拒绝不影响在线设备 |

## 四、保留能力（v1.0 → v1.1 不变或轻改）

- 帧协议：2 字节大端长度前缀 + UTF-8 JSON，负载 ≤1024 字节，连续 3 次畸形包断连，设备侧限速 50 msg/s。
- 心跳：`ping/pong` 带 `seq` 回显；判死超时可配置（`heartbeat_dead_ms`，默认按间隔推导）。
- 异常自愈：WiFi reason code 分类（201/202/205）、指数退避 + 抖动、busy 长退避（6000ms）。
- 事件与证据：`--events-jsonl` 结构化事件作为集成测试唯一机器可读证据。
- 契约机制：三份 `protocol-contract.json`（PC / 设备 / golden）语义一致性强制。
- 工程自包含：`demo/pc` 与 `demo/device` 各自独立构建，集成测试只消费二进制产物。
- 硬件抽象层：`net_backend_t` 29 项 vtable 布局不变（sim/linux/esp32c2 骨架沿用）。

## 五、范围与边界

- 纳入：Windows 10/11 PC（移动热点 + 固定 IP + TCP）、Linux 设备（nmcli 纯 STA）、sim 双进程自动化、UI、配置、契约、事件、测试与文档。
- 不纳入：ESP32-C2 与 ESP-IDF 实机、Linux 上创建 PC 热点、设备侧热点/PIN/凭据下发、mDNS/组播、多设备并发、TLS/云服务/互联网依赖。
- 禁止事项：修改 ESP32 目录；`netsh hostednetwork` 回退；日志/事件输出热点密码；保留隐藏的旧配网/发现主路径；PC 与设备互相引用源码；集成 runner 迁就端侧缺陷。

## 六、验证结论（beta v1.1）

| 项 | 结果 |
|----|------|
| PC 单元 / UI / 契约测试 | 146 用例通过 |
| 设备单元 / 组件测试 | 134 用例通过 |
| sim 双进程集成（A~H） | 全部通过（证据 `out/v11-integration-evidence-final/`） |
| Windows 真实热点 | WinRT 热点启动已实测成功；固定 IP 改写需管理员权限 → 阻塞（CQ7） |
| Linux 真实设备 | 本机无 Linux 环境 → 阻塞（CQ4） |

真实环境两项按 `Document/Agent开发规范.md` 记录为阻塞项，不以 sim 冒充通过；
待提供管理员权限 / Linux 环境后补测。

## 七、历史审核记录

v0~v3 复杂版流程的三轮审核与重设计记录（问题清单 A~G、六维度增强、传输健壮性增强）
已随旧版文档归档至 `archived/流程文档归档/流程审核与重设计说明_v1.0(v1~v3)_归档.md`，
仅供追溯，不再作为当前流程依据。
