# 阶段三完成报告 - beta v1.0.4

> 开发阶段（阶段三）完成，2026-08-05。供阶段四验收与用户检查使用。

## 1. SubStage 完成情况

| SubStage | 内容 | 状态 |
|----------|------|------|
| SubStage_01 | 上位机 app_data 发送链路（核心 + UI + 测试） | ✅ 完成 |
| SubStage_02 | 设备端 app_data 发送链路（核心 + UI + 测试） | ✅ 完成 |
| SubStage_03 | 接口完备性审计 + `demo/硬件抽象层接口规范.md` | ✅ 完成 |
| SubStage_04 | `demo/硬件抽象层移植指南.md` + README 两处更新 | ✅ 完成 |

## 2. 代码变更清单（git diff，17 文件，+290/-2）

| 文件 | 变更 |
|------|------|
| `demo/include/protocol.h` | 仅新增 `#define APP_DATA_TEXT_MAX 512` 一行（验收项 C7 基线） |
| `demo/pc/core/host_tcp_server.h/.cpp` | `QueueAppData`（互斥队列入队）+ `FlushPendingTx`（Poll 末尾冲刷）+ 收包分支展示 `data.text` |
| `demo/pc/core/host_app.h/.cpp` | 门面 `SendAppDataToDevice` |
| `demo/pc/ui/host_window.h/.cpp` | 消息输入框 + "发送消息"按钮 + `SendAppData` 槽（选中行/在线/超长校验） |
| `demo/device/device_priv.h` | 会话区新增 `ui_tx_text[513]`/`ui_tx_pending`/`app_data_seq`（calloc 天然零初始化，无需显式初始化代码） |
| `demo/device/device_app.h/.c` | `device_app_send_app_data`（参数/超长/非会话态拒绝，单槽覆盖记日志） |
| `demo/device/device_session.c` | `session_flush_ui_tx`（session_poll 判死前冲刷，限速重试）+ 收包分支展示 `data.text` |
| `demo/device/ui/device_window.h/.cpp` | 消息输入框 + "发送消息"按钮 + `SendAppData` 槽 |
| `demo/tests/test_host_app.cpp` | 新增 3 用例：`HostAppData.SendToDevice`、`HostAppData.RejectInvalid`、`DeviceAppData.SendToHost`（代 SubStage_02 编写） |
| `demo/tests/test_device_logic.cpp` | 新增 2 用例：`DeviceAppData.RejectWhenNotSession`、`DeviceAppData.RejectEmptyAndTooLong` |
| `demo/README.md` | 硬件抽象层边界说明 + 文档索引小节 |
| `demo/net_esp32c2/README.md` | 顶部移植指南指引行 |

新增文档：`demo/硬件抽象层接口规范.md`（192 行，29 接口逐条契约 + 审计附录）、`demo/硬件抽象层移植指南.md`（230 行，6 章 + 29 接口 ESP-IDF 映射表）。

## 3. 测试结果（单元测试 + 回归）

- 命令：`ctest --test-dir demo/tests/build-msvc -C Debug --output-on-failure`
- 结果：**2/2 测试套件通过，100%**（`provision_demo_tests` 93 用例 + `test_device_ap_backoff`），含本版新增 5 用例全部 PASS。
- 证据：`demo/run/v104_unit_tests.log`。
- 开发中修复的两个测试问题（均只涉及新增测试代码，未改既有测试）：
  1. `DeviceAppData.SendToHost` 等待条件误命中设备自身 `TX` 日志 → 改为等待 host 侧接收日志原文；
  2. E2E 用例排序先于 `DeviceSm.NoCredsGoesToProvision` 且遗留 `run/dev0.nvs.json` 凭据 → 新增用例在 `f.Stop()` 后清理该文件。

## 4. 编译证据

| 工程 | 结果 | 日志 |
|------|------|------|
| `demo/tests`（MSVC Debug） | 0 error | `demo/run/v104_build_tests.log` |
| `demo/pc`（MSVC Debug，含 Qt MOC） | 0 error | `demo/run/v104_build_pc.log` |
| `demo/device`（MSVC Debug，含 Qt MOC） | 0 error | `demo/run/v104_build_device.log` |

## 5. 启动测试证据（验收项 S1/S2 预演）

- 命令（Windows + Qt 6.8.3）：
  - `provision_device.exe --config config\demo_config.json --device-index 0 --fresh --duration 30`
  - `provision_pc.exe --config config\demo_config.json --sim --duration 30`
- 结果：两端窗口正常显示并运行满 30 秒，到点自动优雅关闭（进程自然退出，无崩溃、无残留）。
- 日志证据（0 条 ERROR）：
  - 设备端 `demo/logs/provision_device_dev0_20260805_173539.log`：含"下位机界面已就绪：02:00:00:00:00:01"、状态切换至 ap_provision、热点 Modu_0001 启动；
  - 上位机 `demo/logs/provision_pc_20260805_173539.log`：TCP 监听 0.0.0.0:5935、组播 224.0.2.1:5936、mDNS 注册、"上位机界面已就绪"。
- 新入口可见性：两端"发送消息"按钮与输入框已按设计加入布局（代码位点：`host_window.cpp` buttons 行、`device_window.cpp` controls 行），窗口实际弹出验证由阶段四 S1/S2 人工确认截图。

## 6. 阶段三自决记录（追加至问题清单阶段二自决区之外）

- **Q10（自决，95%）**：新增 E2E 用例对既有 `DeviceSm.NoCredsGoesToProvision` 的 NVS 状态污染，通过"用例结束后清理 `run/dev0.nvs.json`"解决，而非修改既有测试（保护回归基线）。
- **Q11（自决，93%）**：`DeviceAppData.SendToHost` 等待条件采用 host 侧接收日志全句而非裸文本，避免命中发送方自身 TX 日志造成假通过。

## 7. 遗留与说明

- ESP32-C3 真机验证不在本版本范围（验收文档 §1 已声明）；移植指南与接口规范为文档交付，一致性由验收项 D1/D2 核对。
- 全部协议常量未变更（`git diff demo/include/protocol.h` 仅 +1 行宏定义），验收项 C7 可直接复核。
- 阶段三无失败重试项；SubStage 全部一次通过（测试问题修复属正常开发迭代，非任务书失败重试）。

**结论**：阶段三开发完成，具备进入阶段四（验收）条件。
