# 验收报告 - beta v1.0.5 代码完全拆分

> 验收依据：`验收文档.md`。执行环境：Windows 11 x64、VS 2022（MSVC 19.44）、CMake 4.3.2、Qt 6.8.3（msvc2022_64）、Python 3、PowerShell 5.1。Git revision：工作区未提交（用户未要求 commit）。
> 证据目录：`out/ss08-pc`、`out/ss08-device`、`out/ss08-artifacts`、`out/ss08-evidence`（场景 a–h）、`out/ss08-selfcontained`（G1/G2）。

## 一、编译测试

| 项 | 结果 | 证据 |
|---|---|---|
| C1 Windows PC Debug 构建 | ✅ 通过（0 error；`provision_pc.exe` + contract + manifest 生成） | ss08-pc 构建日志 |
| C2 Windows 设备 Debug 构建 | ✅ 通过（0 error；`provision_device.exe` + contract + manifest 生成） | ss08-device 构建日志 |
| C4 ESP-IDF 编译 | 不适用 | 未安装 ESP-IDF 工具链；仅宿主纯 C 语法/接口检查 `esp32_host_check` 通过 | tests/esp32 |
| C3 Linux 构建 | 不适用 | 用户确认本环境不构建 Linux | — |

## 二、启动测试

| 项 | 结果 | 证据 |
|---|---|---|
| H1 Windows offscreen：PC | ✅ 退出码 0，events 含 ready/host_bye_sent/shutdown_complete | ss08 冒烟 JSONL |
| H1 Windows offscreen：设备 | ✅ 退出码 0，events 含 ready/ap_ready/shutdown_complete | 同上 |
| H2 Linux offscreen | 不适用 | 用户确认不构建 Linux | — |
| 场景 c 资源清理 | ✅ 双进程退出 0，无孤儿进程，TCP 5935/20000 可重绑，catalog 清空 | ss08-evidence/c |

## 三、静态与边界验收

| 项 | 结果 | 证据 |
|---|---|---|
| A1 两产品目录完整 | ✅ 各自拥有 CMake/README/源码/config/scripts/tests/cJSON/GoogleTest/install 规则 | 目录清单 |
| A2 CMake 源码边界 | ✅ 两端 CMake 扫描无 `DEMO_ROOT`、无跨根/根级回退引用 | 静态扫描日志 |
| A3 旧根共享生产源码已清理 | ✅ `demo/cmake|common|include|net_*|shared_ui|config|tests|third_party` 已删除，无第三份可编译实现 | git status |
| A4 integration 无私有源码 | ✅ runner 无 include/add_subdirectory/编译端侧源码；G4 二进制隔离通过 | 扫描 + ss08-selfcontained |
| B1 设备语言边界 | ✅ 非 UI 目录（src/backends/firmware）无 `.cpp`、Qt、`std::`、class/new/delete | 扫描日志 |
| B2 UI 仅依赖 C facade | ✅ DeviceWindow 只持 `device_host_t*`；UI 无核心/网络/后端私有头 | include 扫描 |
| B3 facade 生命周期/线程安全 | ✅ runtime 测试覆盖状态矩阵/队列满/日志溢出/快照并发/join 后无写入（部分） | device ctest |
| B4 net_ctx init 失败传播 | ✅ PC/设备副本测试通过，fake backend 返回错误时 out_ctx=NULL | 两端 ctest |

## 四、功能完整性测试（集成场景，双进程 sim）

| 项 | 结果 | 证据 |
|---|---|---|
| E1 protocol manifest 一致 | ✅ 两端 contract 与 golden 语义相等（忽略 generated_from） | protocol-diff.json 空差异 |
| E2 帧互操作与畸形输入 | ✅ 拆包/粘包、1025 超长拒绝、0 长度×3 断连、proto_ver=2 拒绝 | 场景 h |
| F1 首次配网→会话→心跳 | ✅ ap_ready→auth ok→wifi ok→session_online→ping/pong→退出 0 | 场景 b |
| F2 优雅退出与清理 | ✅ host_bye、双 shutdown、无残留 | 场景 c |
| F3 断链重连 | ✅ fault→offline→30s 内上线（reconnect 0→1/1→2）→心跳 | 场景 d |
| F4 close_ap 单次发送失败 | ✅ 失败记录一次、配网成功、最终上线 | 场景 e |
| F5 wifi_result fail | ✅ 无 close_ap_sent、无 session_online、reason=201 真实 | 场景 f |
| F6 双向 app_data | ✅ 512 双向 sha256 一致；513 双向 rejected/payload_too_large 无 tx；UTF-8 一致 | 场景 g |

## 五、整体测试

| 项 | 结果 | 证据 |
|---|---|---|
| D1 PC 全量单测 | ✅ 103/103 | ss08-pc ctest |
| D2 设备全量单测 | ✅ 117/117 | ss08-device ctest |
| D3 Linux fake hotspot | 不适用 | 用户确认不构建 Linux | — |
| D4 README 与测试数量一致 | ✅ README 已改为 103/117 并按平台描述，无固定旧口径 | README 核对 |
| G1 PC 仓库外自包含 | ✅ 副本 configure/build/ctest/offscreen 通过，无仓库路径泄漏 | ss08-selfcontained（PC=True） |
| G2 设备仓库外自包含 | ✅ 副本 117/117 + offscreen 通过；深路径 NVS 不截断 | ss08-selfcontained（Device=True） |
| G3 任意工作目录启动 | ✅ 自包含脚本以空 CWD 启动成功；输出只落显式 runtime 目录 | 同上 |
| G4 integration 二进制隔离 | ✅ 仅 artifacts+integration 副本跑场景 a/b 通过 | SS07 复核记录 |
| J1 README 可执行 | ✅ 三端 README 命令已按文档验证可执行 | 构建记录 |
| J2 必需交付物 | ✅ 需求/问题清单/设计/任务书/验收文档/完成报告/证据齐全 | 目录核对 |

## 六、范围外项（用户决策：本机非测试环境，仅验收 Windows）

> 用户明确：Linux 无需构建，本环境只验收 Windows 下构建与测试。以下项标记为**不适用**（非失败、非阻塞）：

| 项 | 状态 | 说明 |
|---|---|---|
| C3 Linux PC/设备构建 | 不适用 | 用户确认不构建 Linux |
| D3 Linux fake hotspot 测试 | 不适用 | 用户确认不构建 Linux |
| H2 Linux offscreen | 不适用 | 用户确认不构建 Linux |
| C4 ESP-IDF 编译 | 不适用 | 本环境无 ESP-IDF 工具链，且非 Windows 验证范围 |
| I1 Windows 真实 WiFi | 不适用 | 需要专用无线测试环境 |
| I2 Linux 真实热点/DHCP | 不适用 | 需要 Linux + root + AP 网卡 |
| I3 ESP32 实机 | 不适用 | 信息性，不属于本版本通过条件 |

## 七、失败与阻塞处理

- 无失败项。原阻塞项经用户决策全部转为"不适用"（本机为开发机，非测试环境；Linux/真实硬件不在本版本验证范围）。
- 测试使用约束：两端 ctest 与集成场景不得并行运行（共用 127.0.0.1:5935）。

## 八、验收结论

Windows 下全部可执行项 **全部通过**：编译（C1/C2）、启动（H1）、静态/边界（A1–A4、B1–B4）、功能完整性（E/F 场景 a–h）、整体（D1/D2/D4、G1–G4、J）。Linux 与真实硬件项经用户决策不适用。**验收结论：beta v1.0.5 验收通过。**
