# 完成报告 - beta v1.0.5 阶段三（开发）

> Git revision：工作区未提交（用户未要求 commit）。环境：Windows 11，VS 2022（MSVC 19.44），CMake 4.3.2，Qt 6.8.3（msvc2022_64），Python 3，PowerShell 5.1。

## 一、Stage 汇总

| Stage | SubStage | 结果 | 说明 |
|---|---|---|---|
| 1 | SS01 协议契约与迁移基线 | ✅ MainAgent 完成 | golden contract + 迁移清单 |
| 2 | SS02 PC 端自包含工程 | ✅ | 103 单测通过；仓库外自包含验证通过 |
| 2 | SS03 设备 Runtime 与 C Facade | ✅ | 117 单测（含回派修复后） |
| 2 | SS04 设备模拟后端纯 C 化 | ✅ | 56 sim 用例通过；9 个纯 C 模块 |
| 2 | SS05 设备 Linux 与 ESP32 后端 | ✅（Windows 可验部分） | stub/语法检查通过；Linux/IDF 阻塞 |
| 3 | SS06 设备 Qt 外壳与独立构建 | ✅ | 113 单测；事件埋点/scenario 引擎完成 |
| 3 | SS07 端侧测试与跨端 Integration | ✅ | 场景 a–h 全通过；自包含 G1/G2/G4 通过 |
| 4 | SS08 根目录清理、文档与全量回归 | ✅ | 旧根共享目录已删；删除后全量回归通过 |

## 二、回派修复记录

1. SS03：设备配置路径静默截断（`device_config.h` 路径字段 128→512、`device_path.c` 缓冲 1024）+ `QueueFullBusy` 时序测试确定性（`device_runner_test_cmd_hold_ms` 测试钩子）→ 深路径部署 117/117。
2. SS02：`wifi_result` fail 事件 reason 从设备帧真实解析（缺省 -1）+ scenario 延迟锚定到事件到达时刻（QPC 单调毫秒）→ 103/103，场景 g 时序误差 ≤7ms。

## 三、单元与组件测试

- PC：`pc_unit_tests` **103/103 通过**（20 套件，Windows Debug）。
- 设备：设备 CTest **117/117 通过**（runtime/config/core/sim/linux-stub/esp32-check/ui，Windows Debug，3 次串行稳定）。
- 说明：两端 ctest 与集成场景不可并行运行（共用 127.0.0.1:5935），属环境使用约束。

## 四、集成测试（双进程 sim）

场景 a–h 全部通过：manifest 一致性、首次配网→会话→心跳、优雅退出/资源清理、断链重连、close_ap 单次失败、wifi_result fail（reason=201 真实传递）、双向 app_data（512 成功 / 513 rejected / UTF-8）、帧边界与版本拒绝。

## 五、启动测试

- PC offscreen（`--backend sim --duration 3`）：退出码 0，events 含 ready/host_bye_sent/shutdown_complete。
- 设备 offscreen（`--backend sim --device-index 0 --fresh --duration 3`）：退出码 0，events 含 ready/ap_ready/shutdown_complete。
- `--backend linux`（Windows）明确返回 NOT_SUPPORTED（退出码 1），无 sim 回退。

## 六、自包含验收（删除旧根后复验）

- G1 PC：副本 configure/build/ctest/offscreen 通过，无仓库路径泄漏。
- G2 设备：副本 configure/build/ctest（117/117）/offscreen 通过，无泄漏；深路径部署 NVS 不再截断。
- G4 二进制隔离：仅 artifacts+integration 副本跑场景 a、b 通过。

## 七、范围外项（用户决策：本机非测试环境，仅验收 Windows）

| 项 | 说明 |
|---|---|
| Linux PC/设备构建与测试（C3/D3/H2） | 用户明确不构建 Linux，不适用 |
| ESP-IDF build（C4） | 无工具链；宿主 `esp32_host_check` 语法/接口检查已通过 |
| Windows 真实 WiFi / Linux 真实热点 / ESP32 实机（I1/I2/I3） | 需要专用测试环境，不适用；I3 为信息性 |

## 八、遗留问题

- 无未关闭的开发缺陷。设备侧 `--backend linux` create 失败时 `error.message` 为空（facade 未填），qt_main 显示"未知错误"，code=-12 正确；如需中文提示可后续补充。
